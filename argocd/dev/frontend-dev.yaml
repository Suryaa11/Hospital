apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: hms-frontend-dev
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/HospitalManagementSystem-HMS/Hospital-Management.git
    targetRevision: Dev
    path: helm/services/frontend
    helm:
      valueFiles:
        - values-dev.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: hms-dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    
